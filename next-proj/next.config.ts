import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["png.pngtree.com", "images.unsplash.com", "plus.unsplash.com", "comodosslstore.com"]
  }
};

export default nextConfig;
